# .env.ci — template for CI runs (copy values into GitHub Secrets; don't commit real secrets)
# These are read by the CI workflow and/or your app/tests.
# Lines beginning with '#' are ignored by the CI loader step.

# Discord / GCP / Search
DISCORD_BOT_TOKEN=__set_in_actions_secrets__
GCP_PROJECT_ID=your-project-id
GCP_VERTEX_LOCATION=us-central1

# If you use a service account key in CI, store the JSON in secrets.GCP_SA_JSON.
# The workflow writes it to: $GITHUB_WORKSPACE/sa.json and sets GOOGLE_APPLICATION_CREDENTIALS to that path.
GOOGLE_APPLICATION_CREDENTIALS=${GITHUB_WORKSPACE}/sa.json

GOOGLE_CSE_ID=__set_in_actions_secrets__
GOOGLE_CSE_API_KEY=__set_in_actions_secrets__

# Storage / Memory
GCS_BUCKET=fibz-ci-bucket
CHROMA_PATH=.chroma

# Search filtering (optional, comma-separated)
ALLOW_DOMAINS=
DENY_DOMAINS=

# Any other config flags your tests might read:
# FIBZ_ENABLE_OCR=false
# FIBZ_MAX_DISCORD_MESSAGE_CHARS=1800
